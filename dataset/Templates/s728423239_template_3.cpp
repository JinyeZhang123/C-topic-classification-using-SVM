template<class InputIterator, class F>
// bool all_of_index(InputIterator first, InputIterator last, F pred) {
//   while (first != last) {
//     if (!pred(*first)) return false;
//     ++first;
//   }