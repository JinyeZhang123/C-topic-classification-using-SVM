template<typename T> 
void merge_set(set<T> &a, set<T> &b) {
  // b ???????????? a ???????????????????°??????????????????????
  if (a.size() < b.size()) swap(a, b);
  // a ??? b ??????????????¨??¨??\????????????
  a.insert(b.begin(), b.end());
  b = a;
  /* b->clear(); */
}