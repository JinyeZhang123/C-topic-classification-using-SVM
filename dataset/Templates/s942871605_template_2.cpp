template <typename T>
void insertion_sort(std::vector<T>* xs)
{
  for (int i = 1; i < xs->size(); ++i) // xs[i-1]?????§?????????????????? ?????§?????????xs[i]???????´??????????
  {
    dump_vector(*xs);
    T target = (*xs)[i];
    int j = i-1;
    while (j >= 0  && target < (*xs)[j]) {
      (*xs)[j+1] = (*xs)[j];
      --j;
    }