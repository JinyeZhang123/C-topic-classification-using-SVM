int main(void) {
    //ifstream ifs;
    //ifs.open("./input.txt", ifstream::in);
    int num;
    //ifs >> num;
    cin >> num;
    int binaryHeap[MAX];
    for(int i = 1; i < num + 1; i++) {
        cin >> binaryHeap[i];
        
    }