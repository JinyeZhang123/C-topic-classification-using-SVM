int main() {
    // cin.tie(0);
    // ios::sync_with_stdio(false);
    // ifstream in("input.txt");
    // cin.rdbuf(in.rdbuf());
    ll n;
    cin >> n;
    ll low = 0, high = n;
    cout << 0 << endl;
    string s;
    cin >> s;
    if (s == "Vacant") {
        return 0;
    }