int main() {
    cin.tie(0);
    ios::sync_with_stdio(false);
    // ifstream in("input.txt");
    // cin.rdbuf(in.rdbuf());
    ll k;
    cin >> k;
    string ans = "";
    rep(i, k) ans += "ACL";
    cout << ans << endl;
    return 0;
}