int main() {
    cin.tie(0);
    ios::sync_with_stdio(false);
    // ifstream in("input.txt");
    // cin.rdbuf(in.rdbuf());
    ll a;
    string s;
    cin >> a >> s;
    if (a >= 3200) {
        cout << s << endl;
    }