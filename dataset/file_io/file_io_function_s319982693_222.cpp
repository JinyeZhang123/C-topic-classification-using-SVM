int main() {
    //ifstream cin("A.in");
    //ofstream cout("A.out");
    int i, j, k;
    ios::sync_with_stdio(false);
    cin >> str + 1;
    int n = strlen(str + 1);
    cin >> k;
    for(i = 1; i <= n; i++) {
        for(j = 0; j <= k; j++) {
            dp[i][i][j] = 1;
        }